{
  "scripts": {
    "test": "flow",
    "build": "flow && babel src --out-dir build && WEBPACK=release webpack",
    "start": "babel src --out-dir build --watch & webpack-dev-server"
  },
  "dependencies": {
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-3": "^6.5.0",
    "flow-bin": "^0.21.0",
    "webpack": "^1.12.13",
    "webpack-dev-server": "^1.14.1"
  }
}
